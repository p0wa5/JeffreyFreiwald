const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			height: {
				'128': '32rem',
				'132': '36rem',
				'136': '38rem',
			}
		}
	},

	plugins: [

	]
};

module.exports = config;
